<div class="row mt-3">
    <div class="col-12">
        <h2 class="mb-3">{{ 'user.panier_list'|trans }}</h2>

        <table class="table table-sm table-hover">
            <thead>
                <tr>
                    <th>{{ 'cart.id'|trans }}</th>
                    <th>{{ 'cart.amount'|trans }}</th>
                    <th>{{ 'cart.date'|trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for p in paniers %}
                    <tr>
                        <td>{{ p.id }}</td>
                        <td>{{ p.amount }} €</td>
                        <td>{{ p.createdAt|date('d/m/Y H:i') }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">
                            <div class="alert alert-info">
                                {{ 'user.paniers_empty'|trans }}
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>